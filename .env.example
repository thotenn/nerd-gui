# Dictation Manager Configuration
# Copy this file to .env and adjust the paths for your system

# Application directory (where this project is located)
APP_DIR=/home/tho/soft/ai/dictation

# nerd-dictation installation directory
NERD_DICTATION_DIR=/home/tho/app_folder/nerd-dictation

# Vosk models directory
MODELS_DIR=/home/tho/.config/nerd-dictation

# Backend configuration (vosk or whisper)
# BACKEND=vosk

# Whisper configuration (only used if BACKEND=whisper)
# WHISPER_MODEL=medium  # tiny, base, small, medium, large
# WHISPER_DEVICE=cuda   # cuda or cpu
# WHISPER_COMPUTE_TYPE=float16  # float16 for GPU, float32 for CPU

# Whisper Audio/VAD Configuration
# WHISPER_DEVICE_INDEX=  # Leave empty for auto-detect, or specify device index (e.g., 10 for pulse)
# WHISPER_SILENCE_DURATION=1.0  # Seconds of silence before processing speech (0.5-2.0 recommended)
# WHISPER_ENERGY_THRESHOLD=0.002  # Microphone sensitivity (0.0001-0.01, higher = less sensitive)
# WHISPER_MIN_AUDIO_LENGTH=0.3  # Minimum audio length in seconds to process (0.3-1.0 recommended)
